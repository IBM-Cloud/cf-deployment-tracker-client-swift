# docker-compose up
app:
  image: ibmcom/swift-ubuntu:4.0
  volumes:
    - .:/cf-deployment-tracker-client-swift
  command: bash -c "cd /cf-deployment-tracker-client-swift && swift package --build-path .build-linux clean && swift build --build-path .build-linux && swift test --build-path .build-linux && swift package --build-path .build-linux clean"
